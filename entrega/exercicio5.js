const readlineSync = require("readline-sync");
const precoProduto = 1.99;
let sequencia = 0

let fraseDigitada = readlineSync.question("Loja Quase Dois - Tabela de Preco :")

for (let produto = 1 ; produto <=50 ; produto ++) {
    if (produto > 0 && produto <= 50 )
    (sequencia ++ && sequencia !== 0)
    
    console.log (sequencia + "- R$" + produto * precoProduto);

}
